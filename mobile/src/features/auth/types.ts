export type { User } from '../../lib/auth';

// TODO: Add a Profile type if you store extra user data in a Supabase `profiles` table.
// export type Profile = {
//   id: string;
//   display_name: string;
//   avatar_url: string | null;
//   created_at: string;
// };
